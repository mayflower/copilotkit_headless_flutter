name: copilotkit_headless_flutter
description: Headless CopilotKit-style Flutter APIs over AG-UI for local tools, state, renderers, and agent transport.
version: 0.1.0
homepage: https://github.com/mayflower/copilotkit_headless_flutter
repository: https://github.com/mayflower/copilotkit_headless_flutter
issue_tracker: https://github.com/mayflower/copilotkit_headless_flutter/issues
topics:
  - ag-ui
  - agents
  - copilotkit
  - flutter
  - llm

environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
